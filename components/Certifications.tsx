"use client";
import { motion } from "framer-motion";
import { certifications } from "@/data/certifications";
import { cardBase, sectionContainer, sectionPadding, SectionHeading, SectionLabel } from "@/components/SectionHeader";
export default function Certifications() { return <section aria-labelledby="certifications-heading" className={sectionPadding}><div className={sectionContainer}><SectionLabel>Credentials</SectionLabel><SectionHeading id="certifications-heading" className="mt-6">Certifications & Qualifications</SectionHeading><motion.ul initial="hidden" whileInView="show" viewport={{ once: true, amount: 0.2 }} variants={{ hidden: {}, show: { transition: { staggerChildren: 0.05 } } }} className="mt-16 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 lg:gap-5">{certifications.map(({ id, title, issuer, status, year }) => <motion.li key={id} variants={{ hidden: { opacity: 0, y: 16 }, show: { opacity: 1, y: 0, transition: { duration: 0.35 } } }} whileHover={{ y: -4, scale: 1.01 }}><article className={`${cardBase} h-full p-6 sm:p-7`}><div className="flex items-start justify-between gap-3"><div className="flex-1"><h3 className="font-semibold tracking-[-0.01em] text-slate-900">{title}</h3><p className="mt-2 text-sm text-slate-600">{issuer}</p></div><span className="inline-flex items-center rounded-full bg-emerald-50 px-2.5 py-0.5 text-xs font-medium text-emerald-700">{status}</span></div><p className="mt-4 border-t border-slate-100 pt-4 text-xs uppercase tracking-[0.2em] text-slate-500">{year}</p></article></motion.li>)}</motion.ul></div></section>; }
